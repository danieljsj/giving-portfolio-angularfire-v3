'use strict';

/**
 * @ngdoc function
 * @name gpApp.controller:SandboxCtrl
 * @description
 * # SandboxCtrl
 * Controller of the gpApp
 */
angular.module('gpApp')
  .controller('SandboxCtrl', function ($scope, taxonomization) {

  	console.log(taxonomization);

  });

'use strict';

/**
 * @ngdoc function
 * @name gpApp.controller:SandboxCtrl
 * @description
 * # SandboxCtrl
 * Controller of the gpApp
 */

angular.module('gpApp')
  .controller('SandboxCtrl', function ($scope, Ref, /*taxonomization*/ $firebaseObject, Auth) {

  	// sandboxy things here. but, really... do you really need to use a sandbox rather than a real mvc? you might regret it...

  });
